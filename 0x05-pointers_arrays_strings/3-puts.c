#include "main.h"


/**
 * _puts - prints a string, followed by a new line, to stdout
 *
 * %str: string parameterto print
 *
 * Return: Nothing
*/

#include <stdio.h>
{
void _puts(char *str)
	while (*str != '\0')
	{
	ptchar(*str);
	str++;
	}
	putchar('\n');
}
